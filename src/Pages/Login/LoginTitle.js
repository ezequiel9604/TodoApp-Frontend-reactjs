
import { Link } from "react-router-dom";

function LoginTitle() {
    return (
        <Link to="/" className="main__login__title">
            ThInGs ToDo
        </Link>
    );
}

export default LoginTitle;